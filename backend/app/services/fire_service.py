import requests

def get_fire_data(latitude: float, longitude: float):
    """
    Placeholder for NASA FIRMS integration.

    Returns simulated fire data until the live connector is added.
    """

    return {
        "source": "NASA FIRMS (placeholder)",
        "nearby_hotspots": 0,
        "risk": "Low"
    }
